language: php

sudo: false

php:
  - 7.0
  - 7.1

cache:
    directories:
        - "$HOME/.composer"

before_script:
  - composer validate
  - composer install --dev --no-interaction --prefer-dist

script: phpunit --coverage-text --verbose